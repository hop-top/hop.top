# Commands Reference: hop.top

## Global Flags

```
--help, -h        Show help
--version         Show version
--verbose, -v     Enable verbose output
--format, -f FMT  Output format (text|json|yaml)
```

## Commands

### `init`

Initialize a new project.

```sh
hop.top init [directory]
```

**Flags:**
- `--template` — Template to use (default: "default")

### `version`

Print version information.

```sh
hop.top version
```

### `help`

Show help for any command.

```sh
hop.top help [command]
```

## Adding Commands

See the architecture docs and language-specific guides
for how to add new commands to hop.top.
